# SPDX-License-Identifier: Apache-2.0

description: |
  AMS AS5047P magnetic rotary sensor

compatible: "ams,as5047p"

include: [sensor-device.yaml, spi-device.yaml]

properties:
  reg:
    required: true

  spi-max-frequency:
    required: true
